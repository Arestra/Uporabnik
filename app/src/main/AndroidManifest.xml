<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aplikacija.organization">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ApplicationMy"
        android:allowBackup="true"
        android:icon="@drawable/not"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActivityDogodkiMain" />
        <activity
            android:name=".ActivityDogodekSkladbe"
            android:label="@string/title_activity_dodajanje_skladb"
            android:theme="@style/AppTheme.NoActionBar">
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.aplikacija.organization.ActivityDogodekSkladbe" />
        </activity>
        <activity
            android:name=".ActivityDogodek"
            android:label="@string/title_activity_kraj__dogodka" />
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".Pregled_Skladb"
            android:label="@string/title_activity_pregled__skladb"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ActivityDogodekIzvajalci"
            android:label="@string/title_activity_izvajalci" />
        <activity
            android:name=".ActivityDogodekIzvajalec"
            android:label="@string/title_activity_pregled_izvajalcev" />
        <activity
            android:name=".Prijava"
            android:label="Organizacija Dogodkov">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Pregled_Udelezb"
            android:label="@string/title_activity_pregled_udelezb" />
        <activity
            android:name=".Skeniranje"
            android:label="@string/title_activity_skeniranje"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_Spreedsheats"
            android:label="@string/title_activity_activity__spreedsheats"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>